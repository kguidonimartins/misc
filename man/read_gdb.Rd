% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_geo.R
\name{read_gdb}
\alias{read_gdb}
\title{Read layers from a file geodatabase (.gdb)}
\usage{
read_gdb(path, layer = NULL, quiet = TRUE, by_bbox = NULL, ...)
}
\arguments{
\item{path}{Path to a \code{.gdb} directory (the folder whose name ends in
\code{.gdb}).}

\item{layer}{If \code{NULL} (default), every layer reported by \code{\link[sf:st_layers]{sf::st_layers()}}
is read. If a character string, only that layer is read; it must exist in
the geodatabase.}

\item{quiet}{Passed to \code{\link[sf:read_sf]{sf::read_sf()}}.}

\item{by_bbox}{If \code{NULL} (default), every feature is read. Otherwise an
\code{sf}, \code{sfc}, \code{bbox} or named numeric vector (\code{xmin}, \code{ymin}, \code{xmax},
\code{ymax}) whose bounding box limits the features read: only features that
intersect it are returned, filtered by GDAL at read time through the
\code{wkt_filter} argument of \code{\link[sf:st_read]{sf::st_read()}}. The bounding box is
reprojected to each layer's CRS; if it has no CRS, its coordinates are
assumed to be in the layer's CRS. If the layer has no CRS it can be
reprojected to (no \code{.prj}, or an undefined \code{LOCAL_CS}), the coordinates
are used as-is with a warning. Features with empty geometry are
dropped. Layers without a geometry column (e.g. a shapefile made of a
\code{.dbf} only, or an attribute table in a GeoPackage) cannot be filtered:
they are read in full with a warning. When set, \code{nrows_aka_features}
reports the number of features read, not the layer total. Cannot be
combined with \code{wkt_filter} in \code{...}.}

\item{...}{Additional arguments passed to \code{\link[sf:read_sf]{sf::read_sf()}}.}
}
\value{
A tibble with columns \code{fpath} (path or GDAL dsn used for the layer),
\code{file_type} (\code{\link[tools:file_ext]{tools::file_ext()}}), \code{layer_name}, \code{geometry_type}, \code{nrows_aka_features},
\code{ncols_aka_fields}, \code{crs_name} (from \code{st_layers()$crs} when available), and
\code{data} (list-column of \link[sf:sf]{sf::sf} objects). Layers are not row-bound; differing CRS are preserved
per row.
}
\description{
Read layers from a file geodatabase (.gdb)
}
\examples{
\donttest{
gdb <- system.file("extdata", "misc_example.gdb", package = "misc")
if (nzchar(gdb) && dir.exists(gdb)) {
  read_gdb(gdb)
  read_gdb(gdb, layer = "OGRGeoJSON")
}
}
}
\seealso{
Other geo-io:
\code{\link[=clean_geo]{clean_geo()}},
\code{\link[=read_geo]{read_geo()}},
\code{\link[=read_kmz]{read_kmz()}},
\code{\link[=read_sf_zip]{read_sf_zip()}}
}
\concept{geo-io}
