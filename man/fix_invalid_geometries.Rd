% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fix_invalid_geometries.R
\name{fix_invalid_geometries}
\alias{fix_invalid_geometries}
\title{Fix invalid geometries in an sf object}
\usage{
fix_invalid_geometries(data)
}
\arguments{
\item{data}{An \link[sf:sf]{sf::sf} object.}
}
\value{
An \link[sf:sf]{sf::sf} object with the same attributes and geometry column
name as \code{data}, with invalid geometries repaired.
}
\description{
Detects invalid geometries with \code{\link[sf:st_is_valid]{sf::st_is_valid()}}, reports them, and
repairs the layer with \code{\link[sf:st_make_valid]{sf::st_make_valid()}}.
}
\details{
Rows with invalid geometries are reported via \code{message()} before
\code{\link[sf:st_make_valid]{sf::st_make_valid()}} is applied to the whole layer. Warnings emitted by
the repair itself are suppressed. Geometries that become empty after the
repair are kept and flagged with a \code{warning()} naming the affected rows.
When every geometry is already valid, \code{data} is returned unchanged and no
repair is attempted.
}
\examples{
\donttest{
crs_pl <- sf::st_crs(3857)
bowtie <- matrix(
  c(0, 0, 1e5, 0, 0, 1e5, 1e5, 1e5, 0, 0),
  ncol = 2L,
  byrow = TRUE
)
x <- sf::st_sf(
  id = "bowtie",
  geometry = sf::st_sfc(sf::st_polygon(list(bowtie)), crs = crs_pl)
)
fix_invalid_geometries(x)
}
}
\seealso{
Other geo-tools:
\code{\link[=intersect_filter_touch]{intersect_filter_touch()}},
\code{\link[=intersect_mask_filter_area]{intersect_mask_filter_area()}},
\code{\link[=quick_map]{quick_map()}},
\code{\link[=remove_geom_holes]{remove_geom_holes()}},
\code{\link[=view_mapview_from_path]{view_mapview_from_path()}}
}
\concept{geo-tools}
