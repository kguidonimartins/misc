% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/serve_plots.R
\name{serve_plots}
\alias{serve_plots}
\title{Serve R plots over HTTP for a remote browser}
\usage{
serve_plots(
  host = NULL,
  port = 0,
  bind = "0.0.0.0",
  token = TRUE,
  quiet = FALSE,
  ...
)
}
\arguments{
\item{host}{Hostname to advertise in the URL. If \code{NULL}, resolved from
configuration and from the Tailscale node name (see Details).}

\item{port}{Port to bind. The default \code{0} lets the operating system pick a
free port, which avoids clashing with other local servers.}

\item{bind}{Address to bind the server to. Defaults to \code{"0.0.0.0"} so the
device answers on every interface; use \code{"127.0.0.1"} to keep it local.}

\item{token}{Access token, passed to \code{\link[httpgd:hgd]{httpgd::hgd()}}. \code{TRUE} (default)
generates one and embeds it in the returned URL.}

\item{quiet}{If \code{TRUE}, do not message the URL.}

\item{...}{Further arguments passed to \code{\link[httpgd:hgd]{httpgd::hgd()}}, such as \code{width},
\code{height}, \code{zoom}, or \code{bg}.}
}
\value{
The plot URL, invisibly, as a character scalar.
}
\description{
Starts an \code{\link[httpgd:hgd]{httpgd::hgd()}} graphics device bound to all interfaces and
returns a URL that is reachable from another device, such as an iPad
running a terminal over SSH or mosh.
}
\details{
\code{httpgd} advertises its server under the local mDNS name (for example
\code{machine.local}). That name only resolves through multicast on the same
physical network, so it is useless from a tablet connected over a VPN and
is the usual reason a pasted URL loads nothing. \code{serve_plots()} replaces
that hostname with one the remote device can actually resolve.

The advertised hostname is resolved in this order:
\enumerate{
\item the \code{host} argument;
\item the \code{MISC_PLOTS_HOST} environment variable;
\item \code{options(misc.plots_host = "...")};
\item the Tailscale MagicDNS name of this machine, read from
\verb{tailscale status --json};
\item the \code{httpgd} default, with a warning that it may not resolve remotely.
}

The device lives as long as the R session. Call \code{\link[grDevices:dev.off]{grDevices::dev.off()}} to
stop it, or \code{\link[=serve_plots_url]{serve_plots_url()}} to recover the URL of a running device.

Binding to \verb{0.0.0.0} exposes the device to every machine that can reach
this host on that port. Keep \code{token = TRUE} unless the network is trusted.
}
\examples{
\donttest{
if (interactive()) {
  serve_plots()
  plot(1:10)

  # pin the hostname when Tailscale is not in use
  serve_plots(host = "192.168.0.10")
}
}
}
\seealso{
\code{\link[=serve_plots_url]{serve_plots_url()}}

Other data-viewers:
\code{\link[=serve_plots_url]{serve_plots_url()}},
\code{\link[=tad_view]{tad_view()}},
\code{\link[=view_excel]{view_excel()}},
\code{\link[=view_in]{view_in()}},
\code{\link[=view_qgis]{view_qgis()}},
\code{\link[=view_vd]{view_vd()}},
\code{\link[=view_vd_nonint]{view_vd_nonint()}}
}
\concept{data-viewers}
