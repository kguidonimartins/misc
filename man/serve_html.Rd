% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/serve_html.R
\name{serve_html}
\alias{serve_html}
\title{Serve HTML widgets and reports to a remote browser}
\usage{
serve_html(
  x,
  host = NULL,
  name = NULL,
  port = NULL,
  bind = "0.0.0.0",
  quiet = FALSE
)
}
\arguments{
\item{x}{A \code{mapview} object, an \code{htmlwidget}, an \code{sf} object (rendered
with \code{\link[mapview:mapview]{mapview::mapview()}}), or a path to an existing \code{.html} file.}

\item{host}{Hostname to advertise in the URL. If \code{NULL}, resolved the same
way as in \code{\link[=serve_plots]{serve_plots()}}.}

\item{name}{Optional label used in the file name, to tell several served
pages apart. Non-alphanumeric characters are replaced with \code{-}.}

\item{port}{Port to bind. If \code{NULL}, a running session server is reused,
otherwise a random free port is chosen.}

\item{bind}{Address to bind the server to. Defaults to \code{"0.0.0.0"} so the
server answers on every interface; use \code{"127.0.0.1"} to keep it local.}

\item{quiet}{If \code{TRUE}, do not message the URL.}
}
\value{
The page URL, invisibly, as a character scalar.
}
\description{
Writes an HTML widget to disk and serves it over HTTP so it can be opened
in a browser on another device, such as an iPad running a terminal over
SSH or mosh. Handles \code{mapview} maps, any \code{htmlwidget} (\code{leaflet},
\code{plotly}, \code{DT}, \code{gt}), and HTML files that are already on disk, such as a
rendered R Markdown or Quarto report.
}
\details{
One static server is started per R session and reused by later calls, so
every URL handed out stays valid and several pages can be compared in
different browser tabs. Files are served from a session directory below
\code{\link[=tempdir]{tempdir()}} and are removed when R exits. Call \code{\link[=serve_html_stop]{serve_html_stop()}} to shut
the server down earlier.

Pages are served on a background thread, so they stay reachable while R is
busy computing.

The URL is messaged on a line of its own and probed once before it is
returned, for the same reasons given under \verb{Troubleshooting a 404} in
\code{\link[=serve_plots]{serve_plots()}}.

Unlike \code{\link[=serve_plots]{serve_plots()}}, a static server has no access token. Page names
carry a random component instead, and directory listing is disabled, so a
URL cannot be guessed from the port alone. Binding to \verb{0.0.0.0} still
exposes the server to every machine that can reach this host, so prefer
\code{bind = "127.0.0.1"} on untrusted networks.

Interactive maps load their base tiles from the internet, so the remote
device needs network access beyond the tunnel to this machine.
}
\examples{
\donttest{
if (interactive()) {
  # a rendered report
  serve_html("output/report.html")

  # any htmlwidget (leaflet, plotly, DT, gt, ...)
  serve_html(leaflet::leaflet())
}
}
}
\seealso{
\code{\link[=serve_map]{serve_map()}}, \code{\link[=serve_html_stop]{serve_html_stop()}}, \code{\link[=serve_plots]{serve_plots()}}

Other data-viewers:
\code{\link[=serve_html_stop]{serve_html_stop()}},
\code{\link[=serve_map]{serve_map()}},
\code{\link[=serve_plots]{serve_plots()}},
\code{\link[=serve_plots_url]{serve_plots_url()}},
\code{\link[=tad_view]{tad_view()}},
\code{\link[=view_excel]{view_excel()}},
\code{\link[=view_in]{view_in()}},
\code{\link[=view_qgis]{view_qgis()}},
\code{\link[=view_vd]{view_vd()}},
\code{\link[=view_vd_nonint]{view_vd_nonint()}}
}
\concept{data-viewers}
