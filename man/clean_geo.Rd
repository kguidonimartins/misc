% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_geo.R
\name{clean_geo}
\alias{clean_geo}
\title{Clean a spatial file and write a normalized copy}
\usage{
clean_geo(path, output, crs = 4326, encoding = "ISO-8859-1", quiet = FALSE)
}
\arguments{
\item{path}{Path to the input spatial file. Must be \code{.zip} (containing
exactly one shapefile), \code{.shp}, \code{.gpkg}, or \code{.geojson}.}

\item{output}{Path to the output file. Required. The extension determines
the output format and must also be one of \code{.zip}, \code{.shp}, \code{.gpkg}, or
\code{.geojson}. Existing files at \code{output} are overwritten.}

\item{crs}{Target coordinate reference system passed to
\code{\link[sf:st_transform]{sf::st_transform()}}. Defaults to EPSG:4326 (WGS84).}

\item{encoding}{Encoding string used when writing shapefile attribute
tables (passed as \code{layer_options = "ENCODING=<encoding>"}). Applies only
to \code{.shp} and \code{.zip} outputs. Defaults to \code{"ISO-8859-1"}.}

\item{quiet}{Logical. If \code{TRUE}, suppress progress messages.}
}
\value{
Invisibly returns the normalized \code{output} path (character).
}
\description{
Reads a spatial file (\code{.zip} containing a single shapefile, \code{.shp}, \code{.gpkg},
or \code{.geojson}), drops Z/M dimensions, replaces non-ASCII characters in every
attribute column, reprojects the geometry to a target CRS and writes the
result to a user-provided \code{output} path. The output format is determined by
the extension of \code{output} and may differ from the input format (for example,
a \code{.shp} can be cleaned and written as \code{.gpkg}).
}
\details{
This function replaces a standalone batch script that cleaned shapefiles
from a client geospatial portal. The non-ASCII replacement step relies on
\code{\link[textclean:replace_non_ascii]{textclean::replace_non_ascii()}}; \code{textclean} lives in \verb{Suggests:}, so the
function stops with an informative error if it is not installed.
}
\examples{
\donttest{
if (requireNamespace("textclean", quietly = TRUE)) {
  z <- system.file("extdata", "misc_example.zip", package = "misc")
  if (nzchar(z) && file.exists(z)) {
    out <- tempfile(fileext = ".zip")
    clean_geo(z, out)

    out_gpkg <- tempfile(fileext = ".gpkg")
    clean_geo(z, out_gpkg)
  }
}
}
}
\seealso{
Other geo-io: 
\code{\link{read_gdb}()},
\code{\link{read_geo}()},
\code{\link{read_kmz}()},
\code{\link{read_sf_zip}()}
}
\concept{geo-io}
