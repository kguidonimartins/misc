% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/intersect_filter_touch.R
\name{intersect_filter_touch}
\alias{intersect_filter_touch}
\title{Keep features that touch a mask layer}
\usage{
intersect_filter_touch(x, y, repair = TRUE)
}
\arguments{
\item{x}{An \link[sf:sf]{sf::sf} object.}

\item{y}{An \link[sf:sf]{sf::sf} mask layer.}

\item{repair}{If \code{TRUE}, apply \code{\link[sf:st_make_valid]{sf::st_make_valid()}} to \code{x} and \code{y} first
(warnings are suppressed per call). Default \code{TRUE}.}
}
\value{
An \link[sf:sf]{sf::sf} subset of \code{x} containing only features that touch \code{y},
preserving the original geometry and attributes of \code{x}.
}
\description{
Returns the rows of \code{x} whose geometries intersect (touch) any geometry in
\code{y}, using \code{\link[sf:st_intersects]{sf::st_intersects()}}. No clipping is performed: geometries are
returned unchanged. Useful for selecting features inside or crossing a
boundary without computing actual geometric intersections.
}
\details{
A feature is kept when \code{lengths(st_intersects(x, y))} is greater than zero,
i.e. when it shares at least one point with any feature in \code{y}. This is the
lightest-touch selection strategy: it does not reproject, clip, or measure
overlap area. Both layers must share the same CRS (or have CRS that
\code{\link[sf:st_intersects]{sf::st_intersects()}} can reconcile); transform them beforehand if needed.
}
\examples{
\donttest{
crs_pl <- sf::st_crs(3857)
ring <- matrix(
  c(0, 0, 1e6, 0, 1e6, 1e6, 0, 1e6, 0, 0),
  ncol = 2L,
  byrow = TRUE
)
y <- sf::st_sf(geometry = sf::st_sfc(sf::st_polygon(list(ring)), crs = crs_pl))
inner <- matrix(
  c(1e5, 1e5, 9e5, 1e5, 9e5, 9e5, 1e5, 9e5, 1e5, 1e5),
  ncol = 2L,
  byrow = TRUE
)
outside <- matrix(
  c(2e6, 2e6, 3e6, 2e6, 3e6, 3e6, 2e6, 3e6, 2e6, 2e6),
  ncol = 2L,
  byrow = TRUE
)
x <- sf::st_sf(
  id = c("in", "out"),
  geometry = sf::st_sfc(
    sf::st_polygon(list(inner)),
    sf::st_polygon(list(outside)),
    crs = crs_pl
  )
)
intersect_filter_touch(x, y)
}
}
\seealso{
Other geo-tools:
\code{\link[=intersect_mask_filter_area]{intersect_mask_filter_area()}},
\code{\link[=quick_map]{quick_map()}},
\code{\link[=view_mapview_from_path]{view_mapview_from_path()}}
}
\concept{geo-tools}
