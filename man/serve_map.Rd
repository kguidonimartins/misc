% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/serve_html.R
\name{serve_map}
\alias{serve_map}
\title{Serve an interactive map to a remote browser}
\usage{
serve_map(x, ..., host = NULL, name = NULL, quiet = FALSE)
}
\arguments{
\item{x}{An \code{sf} object, or anything else \code{\link[mapview:mapview]{mapview::mapview()}} accepts. A
\code{mapview} object is served as is.}

\item{...}{Further arguments passed to \code{\link[mapview:mapview]{mapview::mapview()}}, such as
\code{zcol} or \code{map.types}.}

\item{host}{Hostname to advertise in the URL. If \code{NULL}, resolved the same
way as in \code{\link[=serve_plots]{serve_plots()}}.}

\item{name}{Optional label used in the file name, to tell several served
pages apart. Non-alphanumeric characters are replaced with \code{-}.}

\item{quiet}{If \code{TRUE}, do not message the URL.}
}
\value{
The page URL, invisibly, as a character scalar.
}
\description{
Thin wrapper around \code{\link[=serve_html]{serve_html()}} that renders \code{x} with
\code{\link[mapview:mapview]{mapview::mapview()}} first, so mapview arguments such as \code{zcol} or
\code{map.types} can be passed straight through.
}
\examples{
\donttest{
if (interactive()) {
  pts <- sf::st_sf(
    id = 1:2,
    geometry = sf::st_sfc(
      sf::st_point(c(-49.25, -16.68)),
      sf::st_point(c(-49.30, -16.70)),
      crs = 4326
    )
  )
  serve_map(pts, zcol = "id")
}
}
}
\seealso{
\code{\link[=serve_html]{serve_html()}}, \code{\link[=serve_html_stop]{serve_html_stop()}}

Other data-viewers:
\code{\link[=serve_html]{serve_html()}},
\code{\link[=serve_html_stop]{serve_html_stop()}},
\code{\link[=serve_plots]{serve_plots()}},
\code{\link[=serve_plots_url]{serve_plots_url()}},
\code{\link[=tad_view]{tad_view()}},
\code{\link[=view_excel]{view_excel()}},
\code{\link[=view_in]{view_in()}},
\code{\link[=view_qgis]{view_qgis()}},
\code{\link[=view_vd]{view_vd()}},
\code{\link[=view_vd_nonint]{view_vd_nonint()}}
}
\concept{data-viewers}
